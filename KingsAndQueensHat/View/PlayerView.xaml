<UserControl x:Class="KingsAndQueensHat.View.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid AutoGenerateColumns="False" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20" ItemsSource="{Binding AllPlayers}" RowStyle="{StaticResource Alternating}" AlternationCount="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Width="140" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Gender" Width="50" Binding="{Binding Gender}"/>
                <DataGridTextColumn Header="Skill" Width="72" Binding="{Binding Skill}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
